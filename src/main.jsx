import React from "react";
import { createRoot } from "react-dom/client";
import UTHOutsTrainer from "./UTHOutsTrainer.jsx";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <UTHOutsTrainer />
  </React.StrictMode>
);
